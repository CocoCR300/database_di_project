{
  "configurations": [
    {
        "type": "firefox",
        "name": "localhost:4200",
        "request": "launch",
        "url": "localhost:4200",
        "pathMappings": [
            {
                "url": "http://localhost:4200/src",
                "path": "${workspaceFolder}/src"
            },
            {
                "url": "http://localhost:4200/src",
                "path": "${workspaceFolder}/src/frontend/src"
            }
        ]
    }
]
}
